## Design

The dashboard remains an operational tool: concise text, compact status labels, and details available on demand. Long paths and R2 keys move behind tooltips/info controls. Model consumption details are documented in a Postman collection that targets the existing `list-deployed-models` and `resolve-channel` functions.
