[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
markers =
    browser: needs playwright + a chromium binary (deselect with -m 'not browser')
