# Copy this file to configs/dataset.local.yaml and adjust paths/classes.
# This follows Ultralytics YOLO segmentation dataset config conventions.

path: ../data/processed/advance-seeds-v1
train: images/train
val: images/val
test: images/test

names:
  0: apple
  1: apple_spot
  2: banana
  3: banana_spot
  4: orange
  5: orange_spot

metadata:
  client: Advance Seeds Co., Ltd.
  task: instance-segmentation
  marker_mm: 50.0
  split_policy: split-by-image
